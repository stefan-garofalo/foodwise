{
  "name": "@apps/backend",
  "type": "module",

  "scripts": {
    "dev": "bun run --hot src/index.ts",
    "build": "tsup",
    "start": "node ./dist/index.js"
  },
  "dependencies": {
    "@hono/trpc-server": "^0.3.4",
    "hono": "^4.7.6",
    "@foodwise/auth": "*",
    "@foodwise/api": "*"
  },
  "devDependencies": {
    "@foodwise/tsconfig": "*",
    "@types/bun": "latest",
    "tsup": "^8.0.0"
  }
}
